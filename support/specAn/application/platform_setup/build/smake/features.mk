UNIT_NAME_SUFFIX=_$(UCCP_STANDARD)_$(TARGET)

ifdef RF
UNIT_NAME_SUFFIX:=$(UNIT_NAME_SUFFIX)_$(RF)
endif

ifdef SOC
UNIT_NAME_SUFFIX:=$(UNIT_NAME_SUFFIX)_$(SOC)
endif

ifneq ($(OPERATING_SYSTEM),NONE)
UNIT_NAME_SUFFIX:=$(UNIT_NAME_SUFFIX)_$(OPERATING_SYSTEM)
endif
